{"type":"result","subtype":"success","is_error":false,"api_error_status":null,"duration_ms":209346,"duration_api_ms":210294,"num_turns":29,"result":"Now I have enough to write a complete, sourced inventory. Let me compile it.\n\n---\n\n## oCMS Cache Layer Inventory\n\n*Primary sources: `../llm-wiki-go/wiki/topics/caching.md`, `../llm-wiki-go/wiki/topics/configuration.md`, `../llm-wiki-go/wiki/topics/login-security.md`, `../llm-wiki-go/wiki/topics/rest-api.md`. Source-code fallbacks listed in §Wiki gaps.*\n\n---\n\n### Layer 1 — Config Cache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/config.go` — `ConfigCache` struct |\n| **Backend** | In-process Go map (`sync.RWMutex`); internal `SimpleCache` for stats |\n| **Env vars / defaults** | None specific; `OCMS_CACHE_TTL` / `OCMS_CACHE_MAX_SIZE` do **not** apply here (see note below) |\n| **TTL** | Hardcoded 1 h (`New(time.Hour)`), but the cache is **write-invalidated** — TTL is a backstop only |\n| **Invalidation triggers** | `InvalidateConfig()` called on any site config update; `InvalidateThemeSettings()` is an alias that also calls this; admin routes bypass the cache entirely (`middleware.LoadSiteConfig(db, nil)`) |\n| **Consumers** | `internal/handler/frontend.go` — site name, URL, description, logo, custom CSS, OG image, robots settings, theme settings, MCP version, `/.well-known/security.txt` fields; `internal/seo/` — sitemap URL; any frontend middleware call to `middleware.LoadSiteConfig(db, cacheManager)` |\n\n---\n\n### Layer 2 — Sitemap Cache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/sitemap.go` — `SitemapCache` struct |\n| **Backend** | In-process `[]byte` field |\n| **Env vars / defaults** | None — TTL is hardcoded |\n| **TTL** | Hardcoded 1 h (passed as `time.Hour` from `manager.go:68`); re-generated on expiry or invalidation |\n| **Invalidation triggers** | `InvalidateSitemap()` on page create/update/delete; `InvalidatePage()` also calls this; `InvalidateContent()` calls this; sitemap is keyed by `site_url` config value (since 0.18.1) — changing `site_url` implicitly misses cache |\n| **Consumers** | `internal/handler/frontend.go` — `GET /sitemap.xml` handler calls `cacheManager.GetSitemap()` |\n\n---\n\n### Layer 3 — Page Cache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/pages.go` — `PageCache` struct |\n| **Backend** | Two in-process maps: `bySlug` and `byID`, keyed by context-aware string |\n| **Env vars / defaults** | None — TTL is hardcoded |\n| **TTL** | Hardcoded 1 h; primarily invalidation-driven |\n| **Cache key shape** | `{language}:{role}:{slug}` and `{language}:{role}:{id}`. Role levels: `anonymous`=0, `public`=0, `editor`=1, `admin`=2 |\n| **Invalidation triggers** | `InvalidateContent()` (bulk), `InvalidatePage(id)` (single page + all its language/role variants), `InvalidatePages()` (entire cache); publish/unpublish also triggers invalidation (fixed in 0.18.1) |\n| **Consumers** | `internal/handler/frontend.go` — public page rendering (slug and ID lookups); `internal/api/v2/pages/service.go` — API page reads |\n\n---\n\n### Layer 4 — Menu Cache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/menus.go` — `MenuCache` struct; wired via `internal/service/menu.go` |\n| **Backend** | In-process map `slug → *MenuWithItems` |\n| **Env vars / defaults** | None |\n| **TTL** | Hardcoded 1 h |\n| **Invalidation triggers** | `InvalidateMenus()` on menu or menu-item create/update/delete; `MenuService.InvalidateCache(slug)` can target a single slug |\n| **Consumers** | `internal/service/menu.go` — `GetMenu()` / `GetMenuForLanguage()`; `internal/handler/frontend.go` — theme template menu rendering |\n\n---\n\n### Layer 5 — Language Cache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/languages.go` — `LanguageCache` struct |\n| **Backend** | In-process slices + `code→Language` map |\n| **Env vars / defaults** | None |\n| **TTL** | Hardcoded 1 h |\n| **Invalidation triggers** | `InvalidateLanguages()` on language create/update/delete |\n| **Consumers** | `internal/middleware/auth.go` and frontend middleware for language resolution; `internal/render/render.go` — template rendering needs active language list; `internal/handler/frontend.go` — hreflang generation, language detection |\n\n---\n\n### Layer 6 — Translation Cache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/translations.go` — `TranslationCache` struct |\n| **Backend** | In-process nested map keyed by `{entityType}:{entityID}` |\n| **Env vars / defaults** | None |\n| **TTL** | Hardcoded 1 h |\n| **Invalidation triggers** | `InvalidateTranslations()` (all); `InvalidateTranslation(entityType, entityID)` (single entity) |\n| **Consumers** | `internal/handler/frontend.go` — per-entity translation lookups during page rendering; multi-language content resolution |\n\n---\n\n### Layer 7 — General / SimpleCache\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/manager.go` — `Manager.General` field, type `*SimpleCache` |\n| **Backend** | In-process TTL map |\n| **Env vars / defaults** | None |\n| **TTL** | Hardcoded **5 minutes** (`New(5 * time.Minute)`) |\n| **Invalidation triggers** | `ClearAll()` calls `m.General.Clear()`; TTL-based automatic eviction every 1 minute |\n| **Consumers** | Reserved slot — no call sites outside `internal/cache/` currently use it. Its stats are included in `TotalStats()` and cleared by `ClearAll()` |\n\n---\n\n### Layer 8 — Distributed Cache (Redis or memory fallback)\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/cache/redis.go` / `internal/cache/memory.go` — selected by factory; held as `Manager.Distributed` |\n| **Backend** | Redis when `OCMS_REDIS_URL` is set; otherwise in-process `MemoryCache` (eviction-bounded). Falls back gracefully to memory on Redis connection failure |\n| **Env vars / defaults** | `OCMS_REDIS_URL` (none → memory-only), `OCMS_CACHE_PREFIX` (`ocms:`), `OCMS_CACHE_TTL` (`3600` s), `OCMS_CACHE_MAX_SIZE` (`10000` entries) |\n| **TTL** | `OCMS_CACHE_TTL` — default 3600 s. This is the **only** cache layer where this env var applies |\n| **Invalidation triggers** | `ClearAll()` calls `m.Distributed.Clear()`; no per-key invalidation methods are wired through the Manager for this layer |\n| **Consumers** | Not yet wired to any domain reads — the `Distributed` field is accessible for future use and is health-checked via `/health` |\n\n---\n\n### Layer 9 — API Rate Limiter (in-memory, per API key)\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/middleware/api.go` — `limiterCache[K]` generic type |\n| **Backend** | In-process map of `token-bucket → limiterEntry`, keyed by API key ID |\n| **Env vars / defaults** | `OCMS_API_RATE_LIMIT` (default `100` — requests per minute per key) |\n| **TTL / eviction** | Idle entries evicted after `limiterCacheTTL = 10 min`; cleanup runs every `limiterCacheCleanupInterval = 5 min`; hard cap of `limiterCacheMaxEntries = 10,000` to bound memory |\n| **Invalidation triggers** | Automatic TTL eviction. Cleared on process restart |\n| **Consumers** | `middleware.APIRateLimit` — applied to all `/api/v2/*` routes |\n\n---\n\n### Layer 10 — Login Rate Limiter + Account Lockout (in-memory)\n\n| Field | Value |\n|-------|-------|\n| **Package / file** | `internal/middleware/` — `middleware.NewLoginProtection(LoginProtectionConfig{…})` |\n| **Backend** | In-process: token bucket per source IP for rate limiting; per-email failed-attempt counter and lockout state |\n| **Env vars / defaults** | **None** — all parameters are code constants in `cmd/ocms/main.go`: burst=5, rate=0.5 tok/s, max attempts=5, lockout=15 min, attempt window=15 min |\n| **TTL / eviction** | Lockout state cleared on process restart; doubling back-off up to 24 h cap |\n| **Invalidation triggers** | Successful login clears the failed-attempt counter for that email; restart wipes all state |\n| **Consumers** | `POST /admin/login` handler only |\n\n---\n\n### Session Store (not a cache — listed for completeness)\n\nThe SCS session store (`internal/session/`) is backed by SQLite, not in-memory. It is not part of the `internal/cache/` system and is not invalidated by `ClearAll()`.\n\n---\n\n## Contradictions (verbatim from wiki)\n\nFrom `../llm-wiki-go/wiki/topics/configuration.md` — **Variable catalog completeness**:\n\n> Three sources catalog `OCMS_*` variables with different scope:\n> - [sources/readme.md](../sources/readme.md) — the full reference, ~55+ variables … Omits `OCMS_API_RATE_LIMIT`.\n> - [sources/wiki-configuration.md](../sources/wiki-configuration.md) — ~40 variables … Includes `OCMS_API_RATE_LIMIT` (default `100`). Omits `OCMS_HCAPTCHA_DISABLED` and `OCMS_REQUIRE_EMBED_PROXY_TOKEN`.\n> - [sources/claude.md](../sources/claude.md) — working subset of ~25 variables. Includes `OCMS_API_RATE_LIMIT` (default `100`). Omits most `OCMS_REQUIRE_*` gates.\n>\n> `OCMS_API_RATE_LIMIT` appears in two of three sources but not in the README — that is drift rather than a semantic conflict, and this wiki page documents it under *Security and API* above.\n\nNo contradictions were found in the caching-specific pages (`topics/caching.md`, `sources/docs-caching.md`, `sources/wiki-caching.md`). The two source pages agree on all facts.\n\n---\n\n## Wiki gaps\n\nFacts that required source-code fallback (not available in the wiki):\n\n| Gap | Source location |\n|-----|----------------|\n| TTLs for individual domain caches are hardcoded `1h` per cache, not governed by `OCMS_CACHE_TTL` | `internal/cache/config.go:30`, `pages.go:33`, `menus.go:34`, `languages.go:29`, `sitemap.go:31` |\n| `OCMS_CACHE_TTL` and `OCMS_CACHE_MAX_SIZE` apply **only** to the `Distributed` cache backend, not the domain caches | `cmd/ocms/main.go:738-744` |\n| A 7th cache (`Manager.General`, 5-min TTL) exists but has no active consumers | `internal/cache/manager.go:72` |\n| `Manager.Distributed` (Layer 8) is not yet wired to any domain-cache reads — it's a reserved slot for future distributed coherence | `internal/cache/manager.go:44-60`, grep confirmed no external `m.Distributed` reads |\n| API rate limiter is its own `limiterCache[K]` with 10-min idle eviction and 10,000-entry hard cap | `internal/middleware/api.go:923-932` |\n| Login rate limiter / lockout state are entirely in-process with no env-var configuration | `internal/middleware/api.go` + login-security topic (which was from wiki, confirming no env vars) |","stop_reason":"end_turn","session_id":"416e6946-7981-4705-88bd-cf314cdba972","total_cost_usd":0.67878685,"usage":{"input_tokens":21,"cache_creation_input_tokens":62799,"cache_read_input_tokens":961972,"output_tokens":10266,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":62799,"ephemeral_5m_input_tokens":0},"inference_geo":"","iterations":[{"input_tokens":1,"output_tokens":3354,"cache_read_input_tokens":70827,"cache_creation_input_tokens":3793,"cache_creation":{"ephemeral_5m_input_tokens":0,"ephemeral_1h_input_tokens":3793},"type":"message"}],"speed":"standard"},"modelUsage":{"claude-haiku-4-5-20251001":{"inputTokens":546,"outputTokens":20,"cacheReadInputTokens":0,"cacheCreationInputTokens":0,"webSearchRequests":0,"costUSD":0.0006460000000000001,"contextWindow":200000,"maxOutputTokens":32000},"claude-sonnet-4-6":{"inputTokens":21,"outputTokens":10266,"cacheReadInputTokens":961972,"cacheCreationInputTokens":62799,"webSearchRequests":0,"costUSD":0.6781408500000001,"contextWindow":200000,"maxOutputTokens":32000}},"permission_denials":[],"terminal_reason":"completed","fast_mode_state":"off","uuid":"b28e1711-2fc0-46a0-8287-cf79b5eb56d3"}
