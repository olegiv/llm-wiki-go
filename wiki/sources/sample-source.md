# Sample Source

## Summary

A placeholder source page. Every ingested item from `raw/` gets its own
page here so that claims made elsewhere in the wiki can cite it.

## Sources

- Origin: `raw/sample.txt` (replace with the real raw path once a source
  is actually ingested)
