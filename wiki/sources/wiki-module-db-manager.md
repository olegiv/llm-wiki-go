# wiki/Module:-DB-Manager.md (oCMS)

## Summary

Wiki version of the DB Manager module reference. Near-parity with `docs/dbmanager-module.md`: same feature set (SELECT/INSERT/UPDATE/DELETE/PRAGMA/EXPLAIN/WITH), same admin UI path (`/admin/dbmanager`), same query-history columns, same routes (`GET /admin/dbmanager`, `POST /admin/dbmanager/execute`), same example queries, same best practices.

Differences from `docs/dbmanager-module.md`:

- Omits the `dbmanager_query_history` DDL.
- Omits query-classification prose.
- Omits module-structure directory tree.
- Omits i18n key catalog.
- No troubleshooting or module-active-status-toggle sections.

No factual disagreements.

## Sources

- Origin: `raw/ocms-go.core/wiki/Module:-DB-Manager.md`
