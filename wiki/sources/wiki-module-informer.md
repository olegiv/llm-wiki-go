# wiki/Module:-Informer.md (oCMS)

## Summary

Wiki version of the Informer module reference. Near-parity with `docs/informer-module.md`: same feature set (dismissible, cookie-based, HTML-supporting notification bar with customizable colors), same default colors (`#1e40af` background, `#ffffff` text), same cookie scheme and 365-day expiry, same theme-integration pattern via `{{informerBar}}`, same technical metadata table (module name, DB table, cookie name, admin URL, template function).

Differences from `docs/informer-module.md`:

- Omits the "Technical Details" deep-dive on CSS animation (`@keyframes informer-spin`) and rendering-order specifics.
- Omits the "Translating the Bar Text" prose about global locale files.
- Adds a wikilink to the Theme System page.

No factual disagreements.

## Sources

- Origin: `raw/ocms-go.core/wiki/Module:-Informer.md`
