# wiki/Module:-Sentinel.md (oCMS)

## Summary

Wiki version of the Sentinel module reference. Matches `docs/sentinel-module.md` on every factual point: same feature scope, same middleware position (slot 3 after RequestID + TrustedRealIP), same four-step per-request flow, same settings table, same ban pattern syntax, same default auto-ban path list, same honeypot flow, same GeoIP integration, same database tables, same 9 admin routes, same template functions, same hook payload shape.

Differences from `docs/sentinel-module.md`:

- Cross-links via Obsidian wikilinks to `[[Forms]]` and `[[GeoIP]]` rather than plain-text references.

No factual disagreements.

## Sources

- Origin: `raw/ocms-go.core/wiki/Module:-Sentinel.md`
