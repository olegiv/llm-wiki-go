# docs/deploy-ubuntu-plesk.md (oCMS)

## Summary

Six-line stub pointing readers to the real guide at `scripts/deploy/README.md`. Lists the chapters covered in the relocated guide: server setup and binary installation, site provisioning with `setup-site.sh`, Plesk SSL and Nginx configuration, instance management with `ocmsctl` and systemd, deploy updates (single- and multi-instance), backups, health checks, cron jobs, and a security checklist.

This consolidation to a single canonical guide at `scripts/deploy/README.md` was done in 0.10.2 (`[sources/changelog.md](changelog.md)` notes: "Merge deployment docs into single `scripts/deploy/README.md`").

## Sources

- Origin: `raw/ocms-go.core/docs/deploy-ubuntu-plesk.md`
